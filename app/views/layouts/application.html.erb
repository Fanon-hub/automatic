<!DOCTYPE html>
<html>
  <head>
    <title><%= t(:app_title, default: 'CdpWebManyoTask') %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", media: 'all' %>
    <%= javascript_include_tag 'application', defer: true %>
  </head>

  <!-- ... head ... -->

<body>
  # <nav id="global-nav">
    # <%= link_to t(:tasks_index), tasks_path %>
    # <%= link_to t(:new_task), new_task_path %>
    <nav id="global-nav">
      <%= link_to t(:tasks_index), tasks_path %>
      <%= link_to t(:new_task), new_task_path %>
    </nav>
  # </nav>
  
  # <% if notice %>
  #   <div class="alert alert-info"><%= t("flash.#{controller_name}.#{action_name}.notice", default: notice) %></div>
  # <% elsif alert %>
  #   <div class="alert alert-danger"><%= t("flash.#{controller_name}.#{action_name}.alert", default: alert) %></div>
  # <% end %>

  <% if notice %>
    <div class="alert alert-info"><%= notice %></div>  <!-- Now translated from controller -->
  <% end %>
  <% if alert %>
    <div class="alert alert-danger"><%= alert %></div>
  <% end %>
  <%= yield %>
</body>
</html>