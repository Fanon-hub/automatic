<div>
  <strong>Labels:</strong>
  <% if @task.labels.any? %>
    <% @task.labels.each do |label| %>
      <span class="badge badge-pill badge-info"><%= label.name %></span>
    <% end %>
  <% else %>
    None
  <% end %>
</div>