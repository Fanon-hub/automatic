
<h1><%= t('edit_task_page') %></h1>
<%= form_with(model: @task, local: true) do |form| %>
	<%= render 'form', form: form %>
<% end %>
<%= link_to t('tasks.edit.back'), task_path(@task), class: 'btn btn-secondary ml-2' %>
